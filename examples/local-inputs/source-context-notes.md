# Source Context Notes (Local)

This local fixture approximates a Delphi form with:
- command actions (`Query`, `Save`)
- dense grid interaction area
- status feedback area

Use this file with `ComplexExcelLikeForm.dfm` when testing extraction prompts.
